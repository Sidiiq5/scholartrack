"use client";import Link from "next/link";import {usePathname} from "next/navigation";import {Logo} from "./logo";const links=[["/student","Discover"],["/student/saved","My Trackers"],["/student/notifications","Notifications"],["/student/settings","Settings"]];export function StudentShell({children}:{children:React.ReactNode}){const path=usePathname();return <div className="min-h-screen bg-[#f8fafc]"><header className="sticky top-0 z-40 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 lg:px-8"><Logo/><nav className="hidden items-center gap-1 md:flex">{links.map(([href,label])=><Link key={href} href={href} className={"rounded-xl px-3.5 py-2 text-sm font-medium "+(path===href?"bg-blue-50 text-blue-700":"text-slate-600 hover:bg-slate-50")}>{label}</Link>)}</nav><Link href="/login" className="rounded-xl border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-700">Sign out</Link></div></header>{children}</div>}