import Link from "next/link";import {Icon} from "./icons";
export type Scholarship={id:number;name:string;provider?:any;description?:string;degree_levels?:string[];degree_level?:string;funding_type?:string;country?:any;deadline:string;days_remaining?:number|null;saved?:boolean};
export function ScholarshipCard({s}:{s:Scholarship}){const provider=typeof s.provider==="string"?s.provider:s.provider?.name||"Provider";const country=typeof s.country==="string"?s.country:s.country?.name||"—";const degree=s.degree_levels?.join(" · ")||s.degree_level||"—";return <article className="group flex h-full flex-col rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg"><div className="flex items-start justify-between gap-4"><div><p className="text-[11px] font-semibold uppercase tracking-[.12em] text-slate-400">{provider}</p><h2 className="mt-2 text-lg font-semibold leading-7 text-slate-950">{s.name}</h2></div><button aria-label={"Save "+s.name} className="grid min-h-11 min-w-11 place-items-center rounded-xl border border-slate-200 text-slate-500 hover:bg-blue-50 hover:text-blue-600"><Icon name="bookmark"/></button></div><p className="mt-4 line-clamp-2 text-sm leading-6 text-slate-600">{s.description||"Structured scholarship information, requirements, funding and official source."}</p><div className="mt-4 flex flex-wrap gap-2 text-xs font-semibold"><span className="rounded-full bg-blue-50 px-2.5 py-1 text-blue-700">{degree}</span><span className="rounded-full bg-slate-100 px-2.5 py-1 text-slate-600">{s.funding_type||"Funding varies"}</span><span className="rounded-full bg-slate-100 px-2.5 py-1 text-slate-600">{country}</span></div><div className="mt-5 border-t border-slate-100 pt-4"><p className="text-[11px] font-semibold uppercase tracking-[.1em] text-slate-400">Application deadline</p><p className="mt-1 text-base font-semibold text-slate-950">{s.deadline}</p><p className="mt-1 text-sm font-medium text-blue-600">{s.days_remaining==null?"Closed":s.days_remaining+" days remaining"}</p></div><Link href={"/scholarships/"+s.id} className="mt-auto flex min-h-11 items-center justify-center gap-2 rounded-xl bg-slate-950 px-4 text-sm font-semibold text-white hover:bg-slate-800">View details <Icon name="arrow" size={16}/></Link></article>}
