"use client";import {useEffect,useState} from "react";import {api} from "../../../lib/api";export default function Notifications(){const [items,setItems]=useState<any[]>([]);const [error,setError]=useState("");useEffect(()=>{api<any>("/notifications/").then(x=>setItems(x.results||x||[])).catch(e=>setError(e.message))},[]);return <main className="mx-auto max-w-4xl px-5 py-10 lg:px-8"><h1 className="text-3xl font-semibold">Notifications</h1><p className="mt-2 text-sm text-slate-600">In-app deadline reminders for scholarships you saved.</p>{error?<div className="mt-6 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>:items.length?<div className="mt-8 space-y-3">{items.map(n=><div key={n.id} className="rounded-2xl border border-slate-200 bg-white p-5"><p className="font-semibold">{n.title}</p><p className="mt-1 text-sm text-slate-600">{n.message}</p></div>)}</div>:<div className="mt-8 rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center text-sm text-slate-600">You’re all caught up.</div>}</main>}