"use client";import {useEffect,useState} from "react";import {api} from "../../../lib/api";import {ScholarshipCard,Scholarship} from "../../../components/scholarship-card";export default function Saved(){const [items,setItems]=useState<Scholarship[]>([]);const [error,setError]=useState("");useEffect(()=>{api<any>("/saved/").then(x=>setItems(x.results||x||[])).catch(e=>setError(e.message))},[]);return <main className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><h1 className="text-3xl font-semibold text-slate-950">My Trackers</h1><p className="mt-2 text-sm text-slate-600">Your saved scholarships and next preparation steps.</p>{error?<div className="mt-6 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>:items.length?<div className="mt-8 grid gap-5 lg:grid-cols-3">{items.map(s=><ScholarshipCard key={s.id} s={s}/>)}</div>:<div className="mt-8 rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center"><h2 className="font-semibold">No saved scholarships yet</h2><p className="mt-2 text-sm text-slate-600">Save a scholarship from Discover to keep it here.</p></div>}</main>}