"use client";import {FormEvent,useState} from "react";import {api} from "../../../lib/api";export default function Settings(){const [name,setName]=useState("");const [email,setEmail]=useState("");const [message,setMessage]=useState("");async function save(e:FormEvent){e.preventDefault();try{await api("/settings/",{method:"PATCH",body:JSON.stringify({name,email})});setMessage("Settings saved.")}catch(e:any){setMessage(e.message)}}return <main className="mx-auto max-w-3xl px-5 py-10 lg:px-8"><h1 className="text-3xl font-semibold">Settings</h1><p className="mt-2 text-sm text-slate-600">Manage your profile and account preferences.</p><form onSubmit={save} className="mt-8 rounded-2xl border border-slate-200 bg-white p-6 space-y-5"><label className="block text-sm font-medium">Name<input value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3"/></label><label className="block text-sm font-medium">Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3"/></label><button className="rounded-xl bg-slate-950 px-5 py-3 text-sm font-semibold text-white">Save changes</button>{message&&<p className="text-sm text-slate-600">{message}</p>}</form></main>}