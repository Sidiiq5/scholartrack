"use client";import {useEffect,useState} from "react";import {useParams} from "next/navigation";import {api} from "../../../../lib/api";
export default function Prepare(){const {scholarshipId}=useParams<{scholarshipId:string}>();const [items,setItems]=useState<any[]>([]);const [error,setError]=useState("");useEffect(()=>{api<any>("/scholarships/"+scholarshipId+"/preparation/").then(x=>setItems(x.results||x||[])).catch(e=>setError(e.message))},[scholarshipId]);return <main className="mx-auto max-w-4xl px-5 py-10 lg:px-8"><p className="text-sm font-medium text-blue-600">Preparation</p><h1 className="mt-1 text-3xl font-semibold">Get ready for this scholarship</h1><p className="mt-2 text-sm text-slate-600">Use Ready, In Progress or Need. ScholarTrack does not store application documents.</p>{error&&<div className="mt-6 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}<div className="mt-8 space-y-3">{items.map(i=><div key={i.id} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex items-center justify-between"><p className="font-semibold">{i.title}</p><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700">{i.status}</span></div><p className="mt-2 text-sm text-slate-500">{i.note||"No note"}</p></div>)}</div>{!items.length&&!error&&<div className="mt-8 rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center text-sm text-slate-500">No preparation items yet.</div>}</main>}
