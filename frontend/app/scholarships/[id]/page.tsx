"use client";import {useEffect,useState} from "react";import {useParams} from "next/navigation";import {PublicNav} from "../../../components/public-nav";import {api} from "../../../lib/api";
export default function Detail(){const {id}=useParams<{id:string}>();const [s,setS]=useState<any>();const [error,setError]=useState("");useEffect(()=>{api<any>("/scholarships/"+id+"/").then(setS).catch(e=>setError(e.message))},[id]);return <><PublicNav/><main className="mx-auto max-w-5xl px-5 py-10 lg:px-8">{error?<div className="rounded-2xl border border-red-200 bg-red-50 p-5 text-sm text-red-700">{error}</div>:!s?<div className="h-96 animate-pulse rounded-2xl bg-slate-100"/>:<article className="rounded-3xl border border-slate-200 bg-white p-7 lg:p-10"><p className="text-sm font-semibold text-blue-600">{s.provider?.name||s.provider}</p><h1 className="mt-2 text-3xl font-semibold">{s.name}</h1><p className="mt-2 text-sm text-slate-500">{s.country?.name||s.country} · {s.status}</p><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl bg-slate-50 p-5"><p className="text-xs text-slate-500">Funding</p><p className="mt-1 font-semibold">{s.funding_type||"—"}</p></div><div className="rounded-2xl bg-slate-50 p-5"><p className="text-xs text-slate-500">Deadline</p><p className="mt-1 font-semibold">{s.deadline}</p></div><div className="rounded-2xl bg-slate-50 p-5"><p className="text-xs text-slate-500">Days remaining</p><p className="mt-1 font-semibold">{s.days_remaining==null?"Closed":s.days_remaining}</p></div></div><section className="mt-10"><h2 className="text-lg font-semibold">Overview</h2><p className="mt-2 text-sm leading-7 text-slate-600">{s.description||"No overview provided."}</p></section><section className="mt-8"><h2 className="text-lg font-semibold">Requirements</h2><div className="mt-3 space-y-2">{(s.requirements||[]).map((r:any)=><div key={r.id} className="rounded-xl border border-slate-200 p-4"><p className="font-medium">{r.title}</p><p className="mt-1 text-sm text-slate-600">{r.description}</p></div>)}</div></section><div className="mt-10 flex flex-wrap gap-3 border-t border-slate-200 pt-6"><a href={s.official_application_url} target="_blank" rel="noreferrer" className="rounded-xl bg-blue-600 px-5 py-3 text-sm font-semibold text-white">Apply on official site ↗</a><a href={s.official_source_url} target="_blank" rel="noreferrer" className="rounded-xl border border-slate-200 px-5 py-3 text-sm font-semibold">Official source ↗</a></div><p className="mt-5 text-xs text-slate-500">Last verified: {s.last_verified||"—"}</p></article>}</main></>}
