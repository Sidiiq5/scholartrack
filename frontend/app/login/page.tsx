"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";import {Logo} from "../../components/logo";import {api} from "../../lib/api";
export default function Login(){const router=useRouter();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{const me=await api<any>("/auth/login/",{method:"POST",body:JSON.stringify({email,password})});router.push(me?.role==="ADMIN"?"/admin":"/student")}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="min-h-screen bg-[#0B0F19] px-4 py-6 text-white"><div className="mx-auto flex min-h-[90vh] max-w-5xl flex-col"><Logo dark/><div className="flex flex-1 items-center justify-center py-10"><div className="w-full max-w-md rounded-2xl border border-white/10 bg-white/[.06] p-6 shadow-2xl backdrop-blur-xl sm:p-8"><div className="text-center"><h1 className="text-2xl font-bold">Welcome back</h1><p className="mt-2 text-sm text-slate-400">Sign in to manage your scholarship discovery and preparation.</p></div>{error&&<div className="mt-6 rounded-xl border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-300">{error}</div>}<form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-xs font-semibold uppercase tracking-wider text-slate-300">Email<input required value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="email" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3.5 py-3 text-sm outline-none focus:border-blue-500"/></label><label className="block text-xs font-semibold uppercase tracking-wider text-slate-300">Password<input required value={password} onChange={e=>setPassword(e.target.value)} type="password" autoComplete="current-password" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3.5 py-3 text-sm outline-none focus:border-blue-500"/></label><button disabled={busy} className="w-full rounded-xl bg-blue-600 py-3 text-sm font-semibold hover:bg-blue-500 disabled:opacity-60">{busy?"Signing in…":"Log In"}</button></form><div className="mt-6 border-t border-white/10 pt-5 text-center text-sm text-slate-400">Don’t have an account? <Link href="/login" className="font-semibold text-blue-400">Create an account</Link></div></div></div></div></main>}