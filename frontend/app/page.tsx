"use client";
import {useEffect,useState} from "react";import {PublicNav} from "../components/public-nav";import {Scholarship,ScholarshipCard} from "../components/scholarship-card";import {Icon} from "../components/icons";import {api} from "../lib/api";
export default function Home(){const [items,setItems]=useState<Scholarship[]>([]);const [q,setQ]=useState("");const [loading,setLoading]=useState(true);const [error,setError]=useState("");useEffect(()=>{let live=true;api<any>("/scholarships/").then(x=>{if(live)setItems(x.results||x||[])}).catch(e=>{if(live)setError(e.message)}).finally(()=>live&&setLoading(false));return()=>{live=false}},[]);const filtered=items.filter(s=>!q||s.name.toLowerCase().includes(q.toLowerCase()));return <main className="min-h-screen bg-white"><PublicNav/><section className="border-b border-slate-200 bg-white"><div className="mx-auto max-w-7xl px-5 pb-10 pt-14 lg:px-8 lg:pb-14"><div className="max-w-3xl"><span className="inline-flex rounded-full border border-blue-100 bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700">Scholarship discovery, simplified</span><h1 className="mt-4 text-4xl font-semibold tracking-[-.035em] text-slate-950 lg:text-[46px] lg:leading-tight">Find scholarships that fit your future.</h1><p className="mt-4 max-w-2xl text-base leading-7 text-slate-600">Discover structured opportunities, understand the requirements, and keep the ones that matter within reach.</p></div><div className="mt-8 max-w-4xl"><div className="flex min-h-14 items-center gap-3 rounded-2xl border border-slate-300 bg-white px-4 shadow-sm focus-within:border-blue-500 focus-within:ring-4 focus-within:ring-blue-500/10"><span className="text-slate-400"><Icon name="search"/></span><input value={q} onChange={e=>setQ(e.target.value)} type="search" placeholder="Search scholarships, fields, universities..." className="min-w-0 flex-1 bg-transparent text-sm text-slate-900 outline-none sm:text-base"/><button className="hidden min-h-10 rounded-xl bg-blue-600 px-5 text-sm font-semibold text-white sm:inline-flex sm:items-center">Search</button></div></div><div className="mt-5 flex gap-3 overflow-x-auto pb-1">{["Degree level","Field of study","Country","Funding","Deadline","Language"].map(x=><button key={x} className="min-h-11 shrink-0 rounded-xl border border-slate-200 bg-white px-4 text-sm font-medium text-slate-700">{x}⌄</button>)}</div></div></section><section id="scholarships" className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><div className="flex items-end justify-between border-b border-slate-200 pb-5"><div><p className="text-sm font-medium text-blue-600">Explore opportunities</p><h2 className="mt-1 text-2xl font-semibold text-slate-950">Scholarships</h2></div><span className="text-sm text-slate-500">{items.length} opportunities</span></div>{loading?<div className="mt-7 grid gap-5 lg:grid-cols-3">{[1,2,3].map(x=><div key={x} className="h-80 animate-pulse rounded-2xl bg-slate-100"/></div>:error?<div className="mt-7 rounded-2xl border border-red-200 bg-red-50 p-6 text-sm text-red-700">{error}</div>:filtered.length===0?<div className="mt-7 rounded-2xl border border-slate-200 bg-slate-50 p-10 text-center"><h3 className="font-semibold text-slate-950">No scholarships found</h3><p className="mt-2 text-sm text-slate-600">Try another search or check back when more opportunities are published.</p></div>:<div className="mt-7 grid gap-5 lg:grid-cols-3">{filtered.map(s=><ScholarshipCard key={s.id} s={s}/>)}</div>}</section><section id="how-it-works" className="border-t border-slate-200 bg-slate-50"><div className="mx-auto max-w-7xl px-5 py-12 lg:px-8"><p className="text-sm font-semibold text-blue-600">The ScholarTrack flow</p><h2 className="mt-2 text-2xl font-semibold text-slate-950">From discovery to a clear next step.</h2><p className="mt-3 max-w-xl text-sm leading-6 text-slate-600">ScholarTrack organizes the information. The official scholarship provider remains the source of truth.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{["Discover","Understand","Save","Prepare"].map((x,i)=><div key={x} className="rounded-2xl border border-slate-200 bg-white p-5"><span className="text-xs font-semibold text-slate-400">0{i+1}</span><h3 className="mt-5 font-semibold text-slate-950">{x}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{["Search and filter opportunities.","See funding, deadlines and requirements clearly.","Keep promising opportunities in your personal shortlist.","Know what you need before the official application."][i]}</p></div>)}</div></div></section><footer className="bg-[#0B0F19]"><div className="mx-auto flex max-w-7xl flex-col gap-3 px-5 py-8 text-sm text-slate-400 sm:flex-row sm:items-center sm:justify-between lg:px-8"><span className="text-lg font-semibold text-white">Scholar<span className="text-blue-400">Track</span></span><span>Find scholarships. Understand them. Save them. Prepare for them.</span></div></footer></main>}
