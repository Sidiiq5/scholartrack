"use client";import {useEffect,useState} from "react";import Link from "next/link";import {api} from "../../../lib/api";export default function Scholarships(){const [items,setItems]=useState<any[]>([]);const [error,setError]=useState("");useEffect(()=>{api<any>("/scholarships/").then(x=>setItems(x.results||x||[])).catch(e=>setError(e.message))},[]);return <section><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-semibold">Scholarships</h1><p className="mt-2 text-sm text-slate-600">Manage scholarship content and lifecycle.</p></div><button className="rounded-xl bg-blue-600 px-4 py-3 text-sm font-semibold text-white">Add scholarship</button></div>{error&&<div className="mt-6 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}<div className="mt-8 overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="grid grid-cols-[2fr_1fr_1fr_1fr] border-b border-slate-200 px-5 py-3 text-xs font-semibold uppercase tracking-wide text-slate-400"><span>Scholarship</span><span>Status</span><span>Deadline</span><span>Action</span></div>{items.map(x=><div key={x.id} className="grid grid-cols-[2fr_1fr_1fr_1fr] items-center border-b border-slate-100 px-5 py-4 text-sm"><span className="font-medium">{x.name}</span><span>{x.status||"—"}</span><span>{x.deadline||"—"}</span><Link href={"/admin/scholarships/"+x.id} className="text-blue-600 font-semibold">View</Link></div>)}{!items.length&&!error&&<div className="p-10 text-center text-sm text-slate-500">No scholarship records available.</div>}</div></section>